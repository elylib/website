{
  "name": "webfiles",
  "version": "1.0.0",
  "description": "Custom JS and CSS for LibGuides",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ed Hill",
  "license": "ISC",
  "devDependencies": {
  }
}
